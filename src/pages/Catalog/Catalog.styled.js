import styled from "styled-components";

const LoadMoreBtn = styled.button` 
    display: block;
    margin: 100px auto 0 auto;
`;

const BtnWrapper = styled.div` 
    display: flex;

    width: fit-content;
    gap: 24px;

    margin: auto;

`;

export { LoadMoreBtn, BtnWrapper };